function zyonDatePicker(elem) {

    if (elem) {

        const datepicker = new Datepicker(elem, {

            // allowOneSidedRange: true,
            // autohide: true,
            showOnFocus: false,
            showOnClick: true,
            todayHighligh: false,
            language: 'es',
            // title: 'Zyon',
            // format: 'dd/mm/yyyy'

        })


    }

}
